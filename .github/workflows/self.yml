name: Test Period Changelog
on:
  workflow_dispatch:
jobs:
  self: # Note: Uses 'dist' build.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: ./
